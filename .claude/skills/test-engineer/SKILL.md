---
name: test-engineer
description: DADAプロセスのPhase 2（総合テスト仕様策定）で使用するペルソナスキルです。承認済みの要求仕様書（SW105）に基づき、設計に先立ってソフトウェア総合テスト仕様書・報告書（SWP6）を作成・修正します。要求の曖昧さを検出した場合はPhase 1へ差し戻します。
---

# test-engineer（Claude Code 用の案内）

このファイルは Claude Code にスキルを自動発見させるための**案内**です。スキルの本文ではありません。

**今すぐ `.agents/skills/test-engineer/SKILL.md` を読み、その手順に従ってください。** 本文・禁止事項・完了条件はすべてそちらが正です。

付属ファイル（本文から参照される。パスはリポジトリ直下からの相対）:

- `.agents/skills/test-engineer/references/SWP6_skeleton.md`

- 本文中の相対パス（`references/...` など）は `.agents/skills/test-engineer/` を起点に解決してください。
- 共通ルールは `.agents/AGENTS.md` です（`CLAUDE.md` から読み込み済みのはずですが、未読なら先に読んでください）。
- 保守メモ: 上の `description` は `.agents/skills/test-engineer/SKILL.md` の `description` と同一に保つこと（正は `.agents/` 側）。

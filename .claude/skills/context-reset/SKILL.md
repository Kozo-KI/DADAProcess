---
name: context-reset
description: 【必須発動】DADAプロセスのフェーズ移行時や承認ゲート／自律ゲート通過時に、過去の不確実な推測を洗浄するためのリセットスキルです。フェーズサマリーをファイルに書き出し、次フェーズをクリーンなコンテキスト（サブエージェント、新しい会話、または同一会話内の論理リセット）で開始します。
---

# context-reset（Claude Code 用の案内）

このファイルは Claude Code にスキルを自動発見させるための**案内**です。スキルの本文ではありません。

**今すぐ `.agents/skills/context-reset/SKILL.md` を読み、その手順に従ってください。** 本文・禁止事項・完了条件はすべてそちらが正です。

- 本文中の相対パス（`references/...` など）は `.agents/skills/context-reset/` を起点に解決してください。
- 共通ルールは `.agents/AGENTS.md` です（`CLAUDE.md` から読み込み済みのはずですが、未読なら先に読んでください）。
- 保守メモ: 上の `description` は `.agents/skills/context-reset/SKILL.md` の `description` と同一に保つこと（正は `.agents/` 側）。

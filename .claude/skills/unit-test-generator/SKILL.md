---
name: unit-test-generator
description: DADAプロセスのPhase 4（実装）でユニットテストをAI内にカプセル化（隠蔽）して、自律的に生成・実行・修復を行うための作業手順書です。
---

# unit-test-generator（Claude Code 用の案内）

このファイルは Claude Code にスキルを自動発見させるための**案内**です。スキルの本文ではありません。

**今すぐ `.agents/skills/unit-test-generator/SKILL.md` を読み、その手順に従ってください。** 本文・禁止事項・完了条件はすべてそちらが正です。

- 本文中の相対パス（`references/...` など）は `.agents/skills/unit-test-generator/` を起点に解決してください。
- 共通ルールは `.agents/AGENTS.md` です（`CLAUDE.md` から読み込み済みのはずですが、未読なら先に読んでください）。
- 保守メモ: 上の `description` は `.agents/skills/unit-test-generator/SKILL.md` の `description` と同一に保つこと（正は `.agents/` 側）。

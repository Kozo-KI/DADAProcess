---
name: requirements-engineer
description: DADAプロセスのPhase 1（要求定義）で使用するペルソナスキルです。Product Ownerとの壁打ちを通じて要求を引き出し、ソフトウェア要求仕様書（SW105）を作成・修正します。
---

# requirements-engineer（Claude Code 用の案内）

このファイルは Claude Code にスキルを自動発見させるための**案内**です。スキルの本文ではありません。

**今すぐ `.agents/skills/requirements-engineer/SKILL.md` を読み、その手順に従ってください。** 本文・禁止事項・完了条件はすべてそちらが正です。

付属ファイル（本文から参照される。パスはリポジトリ直下からの相対）:

- `.agents/skills/requirements-engineer/references/SW105_skeleton.md`

- 本文中の相対パス（`references/...` など）は `.agents/skills/requirements-engineer/` を起点に解決してください。
- 共通ルールは `.agents/AGENTS.md` です（`CLAUDE.md` から読み込み済みのはずですが、未読なら先に読んでください）。
- 保守メモ: 上の `description` は `.agents/skills/requirements-engineer/SKILL.md` の `description` と同一に保つこと（正は `.agents/` 側）。

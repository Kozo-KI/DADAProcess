---
name: programmer
description: DADAプロセスのPhase 4（実装・総合テスト報告）で使用するペルソナスキルです。承認済みの設計書（SW205）に忠実にソースコードを実装し、SWP6の全テストを実施して報告書を締結し、バグ管理表（BUG101）へ記録します。
---

# programmer（Claude Code 用の案内）

このファイルは Claude Code にスキルを自動発見させるための**案内**です。スキルの本文ではありません。

**今すぐ `.agents/skills/programmer/SKILL.md` を読み、その手順に従ってください。** 本文・禁止事項・完了条件はすべてそちらが正です。

- 本文中の相対パス（`references/...` など）は `.agents/skills/programmer/` を起点に解決してください。
- 共通ルールは `.agents/AGENTS.md` です（`CLAUDE.md` から読み込み済みのはずですが、未読なら先に読んでください）。
- 保守メモ: 上の `description` は `.agents/skills/programmer/SKILL.md` の `description` と同一に保つこと（正は `.agents/` 側）。
